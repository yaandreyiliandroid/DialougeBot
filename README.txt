## Моя идея для проекта:
> 1)бот с которым можно пообщаться
> 2)дать советы по решению проблем по поводу глобалного потепления
> 3)и посмеяться от мемов

Этот дискорд бот даёт советы и мемы.

## Тип моего проекта:
> 

## Библиотека, которую я буду использовать
-discord

## Референсы, которые мне пригодятся
- Референс 1:
код из дискорд бота кторый отвечает на команды

- Референс 2:
@bot.command()
async def mem(ctx):
    images = os.listdir('images')
    img_name = random.choice(images)
    with open(f'images/{img_name}', 'rb') as f:
        # В переменную кладем файл, который преобразуется в файл библиотеки Discord!
        picture = discord.File(f)
   # Можем передавать файл как параметр!
    await ctx.send(file=picture)
код с помощью которого можно отправлять картинки





